Friend Module Dimensions
    Friend HEADING As String = NameOf(HEADING)
    Friend SPEED As String = NameOf(SPEED)
    Friend LONGITUDE As String = NameOf(LONGITUDE)
    Friend LATITUDE As String = NameOf(LATITUDE)
End Module
